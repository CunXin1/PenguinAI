# torch / torchvision are installed in ml/Dockerfile (not here) so the variant
# can be chosen at build time: CPU wheels by default (builds on Mac/ARM + CPU
# Linux), CUDA on a GPU host via --build-arg TORCH_SPEC / TORCH_INDEX_URL.

transformers==4.47.1
sentence-transformers==3.3.1
xgboost==2.1.3
scikit-learn==1.5.2
pandas==3.0.3
pandas-ta==0.3.14b0
numpy==1.26.4
duckdb==1.1.3
celery[redis]==5.4.0
redis==5.2.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pydantic-settings==2.7.0
httpx==0.28.1
ib_insync==0.9.86
praw==7.8.1
playwright==1.49.0
polygon-api-client==1.16.3
python-dotenv==1.2.2
mlflow==3.13.0
